# large vendor binary CAD (kept locally, not in git — see cad/vendor/SOURCES.md)
cad/vendor/*.stp
cad/vendor/*.step
cad/vendor/**/*.stp
cad/vendor/**/*.step

# research scrape cache (raw scraped pages — not part of the project)
.firecrawl/

# scratch
cad/.exp_mod*.scad
*.log
.DS_Store
