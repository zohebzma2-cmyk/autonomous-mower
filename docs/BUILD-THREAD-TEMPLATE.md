# Build-thread post template (one per phase, ~5 min each)

**Title:** [Phase N] <one thing that happened> — autonomous 615 lb ZTR build

1. ONE photo (the money shot — bracket on steel, meter on the kill chain)
2. What got done (2–3 sentences, plain)
3. The surprise (there is always one — this is what people read for)
4. What's next
5. Link: repo + the interactive writeup

Post to: GitHub Discussions (Build logs) first, then the ArduPilot Discourse thread.
Cross-post the best one monthly to r/robotics.
